services:
  axum:
    build: .
    ports:
      - "8000:8000"
  db:
    image: postgres:15.3-alpine3.18
    restart: always
    env_file: "./.env"
    ports:
      - "5432:5432"
    volumes:
      - ./sql/init.sql:/docker-entrypoint-initdb.d/init.sql
